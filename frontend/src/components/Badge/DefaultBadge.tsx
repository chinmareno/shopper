import { Badge } from "@/components/ui/badge";

export const DefaultBadge = () => {
      return (
    <Badge className="bg-primary/10 border border-primary text-primary text-[10px] h-4">
      Default
    </Badge>
  );
};